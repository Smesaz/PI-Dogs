const initialState=[];

const breeds_reucer = (state=initialState, action) =>{
    switch (action.type) {
        case value:
            
            break;
    
        default:
            break;
    }
}
export default breeds_reucer;